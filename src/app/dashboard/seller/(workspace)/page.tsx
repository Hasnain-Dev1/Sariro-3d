import SellerWorkspace from '../seller-workspace';

/** Today: the calls that are late or due, and the workspaces. */
export default function SellerTodayPage() {
  return <SellerWorkspace workspace="today" />;
}
