import StudentWorkspace from '../student-workspace';

/** Today: the next class, and what there is to do. */
export default function StudentTodayPage() {
  return <StudentWorkspace workspace="today" />;
}
