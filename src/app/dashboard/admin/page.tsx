import AdminWorkspace from './admin-workspace';

/** Today: what is waiting, the workspaces, and the totals. */
export default function AdminTodayPage() {
  return <AdminWorkspace workspace="today" />;
}
