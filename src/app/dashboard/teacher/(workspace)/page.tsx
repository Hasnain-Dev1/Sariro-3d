import TeacherWorkspace from '../teacher-workspace';

/** Today: what is waiting, the next class, and the week. */
export default function TeacherTodayPage() {
  return <TeacherWorkspace workspace="today" />;
}
