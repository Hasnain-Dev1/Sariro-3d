import SuperAdminWorkspace from './super-admin-workspace';

/** Today: what is waiting, the workspaces, and system health. */
export default function SuperAdminTodayPage() {
  return <SuperAdminWorkspace workspace="today" />;
}
